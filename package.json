{
  "name": "@alexminza/skills-setup",
  "version": "2026.5.17",
  "description": "OpenClaw plugin for running skill setup scripts.",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist/",
    "src/",
    "openclaw.plugin.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run build",
    "test": "vitest run"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.5"
  },
  "devDependencies": {
    "openclaw": "2026.5.5",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  },
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "runtimeExtensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.5.5",
      "minGatewayVersion": "2026.5.5"
    },
    "build": {
      "openclawVersion": "2026.5.5",
      "pluginSdkVersion": "2026.5.5"
    }
  }
}
